{
  "name": "odaya-token-calculator",
  "version": "1.0.0",
  "private": true,
  "description": "Client-side Hermes token-cost calculator using Nous Portal pricing",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.js",
    "serve": "npx serve dist -l 4173"
  },
  "engines": { "node": ">=18" }
}
